#ifndef QUAD_HPP
#define QUAD_HPP

  #include "framework.hpp"

  class Quad: public geo::FloatPoint
  {
    public:

      Quad(const geo::FloatPoint &position);
      ~Quad();


    private:

      /* Data */
  };

#endif // QUAD_HPP
